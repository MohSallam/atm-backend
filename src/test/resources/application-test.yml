spring:
  datasource:
    url: jdbc:h2:mem:atm-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.SQL: warn

security:
  jwt:
    secret: "test-secret-6f8cbb24c2c64c2ba6d4e4d1978b30ef"
    expiration-seconds: 3600
